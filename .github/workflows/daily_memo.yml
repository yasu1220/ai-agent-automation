# このワークフローはselect_daily_tasks.ymlに統合されたため廃止済み
# GitHubのActionsタブからDisableまたは削除してください
name: 日次メモ自動生成（廃止）

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: このワークフローは廃止されました
        run: echo "このワークフローはselect_daily_tasks.ymlに統合されました。削除してください。"
